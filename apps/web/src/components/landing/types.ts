export type FanCard = {
  title: string;
  color: string;
  dot: string;
  titleColor: string;
  descColor: string;
  rot: number;
  x: number;
  desc: string;
};

export type ServiceNode = {
  title: string;
  desc: string;
};

export type StepItem = {
  id: string;
  title: string;
  sub: string;
  copy: string;
  bg: string;
  card: string;
};
